# ppt-aigen-poc
// LLM's run locally through Ollama, models used (gemma3:1b, deepseek-v3.1:671b-cloud)

// to run install dependencies

npm install;

//declare environment variables and 

export GAMMA_API_KEY={{apiKey}}; export GAMMA_ID={{gammaId}}; export THEME_ID={{themeId}};
node pathToRepo/demo/workflow.demo.js
